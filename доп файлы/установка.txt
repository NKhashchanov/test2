apt update && apt install -y python3 python3-pip postgresql-client

apt install -y apache2

apt install -y software-properties-common

add-apt-repository ppa:ondrej/php -y

apt update

apt install -y php8.2 php8.2-cli php8.2-fpm php8.2-common php8.2-pgsql php8.2-pdo php8.2-pdo-pgsql php8.2-curl php8.2-gd php8.2-mbstring php8.2-xml php8.2-zip php8.2-bcmath

apt install -y libapache2-mod-php8.2

a2enmod php8.2

a2enmod rewrite

a2enmod ssl

systemctl enable apache2

systemctl start apache2




docker inspect sql-trainer | grep -A 5 "Cmd"

docker commit --change='CMD ["apache2ctl", "-D", "FOREGROUND"]' sql-trainer vds:latest

docker run -d -p 8080:80 vds tail -f /dev/null



docker build -t vds .

docker run -p 8080:80 -p 8000:8000 -p 5444:5432 -p 2222:22 -v d:/trainer/html:/var/www/html --name postgresql-trainer vds  